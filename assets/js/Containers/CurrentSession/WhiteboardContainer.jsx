import React from 'react';
import { connect } from 'react-redux';
import Whiteboard from '../../Components/CurrentSession/Whiteboard.jsx'

function mapStateToProps(state) {
    return {

    }
}

export default connect(mapStateToProps)(Whiteboard);