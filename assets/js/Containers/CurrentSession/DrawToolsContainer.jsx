import React from 'react';
import { connect } from 'react-redux';
import DrawTools from '../../Components/CurrentSession/DrawTools.jsx'

function mapStateToProps(state) {
    return {

    }
}

export default connect(mapStateToProps)(DrawTools);