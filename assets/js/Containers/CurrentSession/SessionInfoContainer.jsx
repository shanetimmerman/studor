import React from 'react';
import { connect } from 'react-redux';
import SessionInfo from '../../Components/CurrentSession/SessionInfo.jsx'

function mapStateToProps(state) {
    return {

    }
}

export default connect(mapStateToProps)(SessionInfo);